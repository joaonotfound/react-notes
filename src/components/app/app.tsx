import { AppShell } from "@mantine/core";
import { Header } from "../header/header";

function App() {
  return (
   <AppShell
    header={<Header />}
   > 
   </AppShell>
  );
}

export default App;
